Make sure node.js is installed on your machine, then from the /front-end folder run:
```
npm install
```
after that, you should be able to run:
```
npm start
```
in order to run the web app in your browser.